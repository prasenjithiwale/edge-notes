{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Event listening only. Commands defined by this app are allowed by default; the webview gets no window, filesystem, shell or SQL permissions.",
  "windows": ["dock"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
